import React, { Component } from 'react';
import '../App.css';

export default class SingleGlass extends Component {
    render() {
        return(
            <React.Fragment>
                
            </React.Fragment>
        )
    }
}